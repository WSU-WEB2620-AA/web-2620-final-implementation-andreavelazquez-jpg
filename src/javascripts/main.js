require.context('../fonts/', true, /\.(eot|ttf|woff|woff2)$/i);
require.context('../images/', true, /\.(png|jpg|jpeg|gif|svg|mp4)$/i);
require('../stylesheets/main.scss');

// Optional: just to confirm JS is running
console.log('Wonder Studios JS loaded');